apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  labels:
    app: rabbitmq-service
spec:
  type: ClusterIP
  selector:
    app: rabbitmq-dev
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672 
    - name: management
      port: 15672
      targetPort: 15672